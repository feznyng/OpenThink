import axios from "axios";
import {baseURL} from './index';
import store, { AppDispatch } from '../Store';
import { space } from "../types/space";
import {post, comment, vote} from "../types/post";

export const createPost = () => async (dispatch: AppDispatch) => {
}
  
  
export const mountPostListeners = () => async (dispatch: AppDispatch) => {

}

  
  
export const unmountPostListeners = () => async (dispatch: AppDispatch) => {
    
    
}
  