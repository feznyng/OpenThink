import React from 'react'
import { DatabaseViewProps } from '../../types/database';
import { TaskViewProps } from '../../types/task'


export default function TaskTable(props: DatabaseViewProps) {

    return (
        <div>
        </div>
    )
}
