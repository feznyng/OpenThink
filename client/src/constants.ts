export const MAX_USER_SPACES = 500;
export const MAX_USER_FETCH_COUNT = 500;

export const MAX_TAGS = 20; // a post can have a maximum of 20 tags
export const MAX_REACTIONS = 6; // will be extended when reactions menu is deployed
export const MAX_ATTRIBUTES = 1600; // based on max number of columns in Postgres

export const completeColor = '#5DB321'